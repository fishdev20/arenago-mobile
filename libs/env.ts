// import 'dotenv/config';

// function must(k: string) {
//   const v = process.env[k];
//   if (!v) throw new Error(`Missing env ${k}`);
//   return v;
// }

// export const env = {
//   API_PORT: Number(process.env.PORT || 4000),
//   API_BASE: must('API_BASE'),
// };
